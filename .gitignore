# Real credentials -- never commit
config.env
config.env.bak
# Discovery output can contain hostnames, tokens, cluster internals
discover-output.txt
diag-*/
diag-*.tgz
*.log
*.tmp

# Rendered manifests (may contain substituted secrets)
rendered/
*.rendered.yaml

# OS noise
.DS_Store
Thumbs.db
.writetest

# MaaS pre-teardown backups (contain provider API keys)
.maas-backup/
