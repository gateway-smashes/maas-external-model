# Manifests use ${VAR} placeholders resolved by envsubst in scripts/10-apply.sh.
# Plain `kubectl apply -k` will NOT work directly.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 20-externalmodel.yaml
  - 30-maas-access.yaml
